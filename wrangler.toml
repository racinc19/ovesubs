# Cloudflare Pages: git deploys use this as the static output directory (see dashboard → Build → Wrangler file).
name = "ove-subs"
pages_build_output_dir = "./Deploy/site_ove"
compatibility_date = "2024-11-01"
